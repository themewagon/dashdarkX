import { Box } from "@mui/material";
import Sidebar from "components/Sidebar";

const Dashboard = () => {
    return (
        <Box sx={{padding: '1rem'}}>
            <Sidebar/>
        </Box>
    );
}

export default Dashboard;